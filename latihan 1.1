import numpy as np
import matplotlib.pyplot as plt

x = np.arange(0, 10, 1)
y = x ** 2

plt.plot(x, y)
plt.xlabel("Nilai garis Horizontal")
plt.ylabel("Nilai garis Vertikal")
plt.title("Grafik vertikal = horizontal^2")
plt.show()
